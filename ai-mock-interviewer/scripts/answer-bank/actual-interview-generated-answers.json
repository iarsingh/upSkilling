{
  "explain your day to day linux activities": "I spend most of my time in the terminal, using commands like `git status` to track changes and `grep` to search for specific patterns. I also frequently use `top` or `htop` to monitor system resource utilization and identify potential bottlenecks. When working on a project, I'll often switch between multiple terminals to run concurrent processes, such as building code with `make` while running tests in another tab. To manage my workflow, I use tools like `tmux` for session management and `screen` for logging in remotely.",
  "how do you troubleshoot high cpu utilization": "To troubleshoot high CPU utilization, I first check the system load average with `top` or `htop`. If a specific process is consuming excessive resources, I use `ps aux | grep <process_name>` to identify its PID and then run `strace -p <PID>` to analyze system calls. Next, I examine the process's resource usage with `pmap -d <PID>`. To further diagnose, I may enable kernel logging with `sysctl kern.log_level=7` and review the `/var/log/messages` file for relevant entries. If necessary, I'll restart or terminate the offending process to restore system performance.",
  "how do you identify which process is consuming memory": "To identify which process is consuming memory, use the `top` command. This will display a list of running processes and their corresponding memory usage. You can also use the `ps` command with the `-o` option to specify the output format, for example: `ps -eo pid,ppid,%mem,%cpu --sort=-%mem`. This will show you the process ID, parent process ID, memory usage, and CPU usage of each process, sorted by memory usage in descending order. Be aware that high memory usage can be a sign of a resource-intensive process or a potential memory leak.",
  "explain the linux boot process": "The Linux boot process begins with the Power-On Self-Test (POST) phase, which checks the system's hardware for any issues. If everything is okay, it loads the Master Boot Record (MBR) from the hard drive into memory and transfers control to the bootloader, typically GRUB. The bootloader then reads the kernel image from disk and passes control to it. The kernel initializes the system by loading device drivers, mounting file systems, and setting up network interfaces. Finally, it executes the `/etc/rc.d/*` scripts or systemd services to start system services.",
  "explain systemd": "Systemd is a system and service manager for Linux operating systems. It provides a replacement for System V init and other initialization systems. Systemd manages system services, such as network interfaces, databases, and web servers, by running them in the background and monitoring their status. It also handles system startup and shutdown processes. Systemd uses a hierarchical structure to manage services, with each service represented by a unit file in `/etc/systemd/system/`. The `systemctl` command is used to control systemd services.",
  "what happens when linux boots": "When Linux boots, the following events occur: (1) Power-On Self-Test (POST): checks hardware for issues. (2) Loads Master Boot Record (MBR) from hard drive into memory and transfers control to bootloader. (3) Bootloader reads kernel image from disk and passes control to it. (4) Kernel initializes system by loading device drivers, mounting file systems, and setting up network interfaces. (5) Executes `/etc/rc.d/*` scripts or systemd services to start system services.",
  "difference between hard link and soft link": "A hard link is a reference to the same inode as another file on the filesystem. It's essentially an alias for the original file, and changes made through one link affect the other. A soft link, also known as a symbolic link, is a separate entity that points to the location of another file or directory. When you delete the target of a hard link, the link itself becomes invalid; with a soft link, deleting the target breaks the link.",
  "explain linux permissions": "Linux permissions are based on three types: owner (u), group (g), and others (o). Each type has read (r), write (w), and execute (x) permissions. The `umask` value determines the default permissions for newly created files. For example, if `umask` is set to 022, new files will have `rw-r--r--` permissions. Important trade-offs include balancing security with usability: overly restrictive permissions can hinder collaboration, while too permissive settings may compromise data integrity.",
  "how do you troubleshoot high memory utilization": "To troubleshoot high memory utilization, run `top` or `htop`, sort by memory usage (`%MEM` column), and identify the top processes consuming memory. Use `pmap -d <pid>` to analyze a process's memory map. Check for memory leaks using `valgrind` or `memcheck`. Monitor system logs for errors related to memory allocation.",
  "what is load average": "Load Average measures the average number of processes waiting in the run queue over a specified time period (1, 5, and 15 minutes). It indicates system load and can be used to detect bottlenecks. Use `cat /proc/loadavg` or `uptime` to view current Load Average values.",
  "what does the uptime command show": "The uptime command shows the system's up time, number of users logged in, and system load averages for the past 1, 5, and 15 minutes. It also displays the total number of processes running and the number of processes sleeping or stopped.",
  "difference between top and htop": "`top` displays real-time system information, while `htop` is an interactive version with additional features like sorting, filtering, and searching. Both display process CPU usage, memory consumption, and other metrics.",
  "difference between free and vmstat": "`free` reports the total, used, and free memory, as well as swap space information. `vmstat`, on the other hand, provides detailed statistics about memory usage, including page faults, disk I/O, and CPU utilization.",
  "how do you identify which process is consuming cpu": "To identify which process is consuming CPU, use `top` or `htop` and sort by CPU usage (`%CPU` column). You can also use `ps -eo pcpu,pid,comm` to display a list of processes sorted by CPU usage. Use `pstack <pid>` to analyze the call stack of a process.",
  "explain chmod 755": "`chmod 755` sets permissions for a file or directory: owner has read, write, and execute (`rwx`) permissions, group members have read and execute (`rx`) permissions, and others have read and execute (`rx`) permissions. This is a common permission set for executable files.",
  "explain chmod 777": "`chmod 777` sets permissions for a file or directory: owner has read, write, and execute (`rwx`) permissions, group members have read, write, and execute (`rwx`) permissions, and others have read, write, and execute (`rwx`) permissions. This is an insecure permission set that should be avoided.",
  "difference between sudo and su": "`sudo` allows a user to run a command with superuser privileges for a short period, while `su` switches the current user to another user account. Use `sudo` when you need temporary elevated privileges, and use `su` when you need to switch to a different user account.",
  "explain file descriptors": "File descriptors (FDs) are small integer values used by processes to refer to open files or network connections. Each FD is associated with a file descriptor table entry, which contains information about the corresponding file or connection. Use `lsof` or `netstat` to display open FDs and their corresponding files or connections.",
  "how do you check disk usage": "To check disk usage, use the `df` command. For example: `df -h` will display human-readable file system disk space usage. Be aware that high disk usage can lead to production failure modes such as slow performance and potential data loss.",
  "what logs do you check during troubleshooting": "During troubleshooting, check the `/var/log/syslog`, `/var/log/messages`, and `/var/log/daemon.log` logs for relevant error messages. Be cautious of security concerns when reviewing logs, especially if they contain sensitive information such as passwords or API keys.",
  "how do you find large files": "To find large files, use the `find` command with the `-size` option. For example: `find / -type f -size +100M` will search for files larger than 100 megabytes. Be aware of potential reliability concerns if this command is run on a large file system.",
  "how do you schedule cron jobs": "To schedule cron jobs, use the `crontab` command to edit the crontab file. For example: `crontab -e` will open the editor for the current user's crontab. Be aware of potential security concerns if the cron job is not properly secured.",
  "how do you automate linux administration": "To automate Linux administration, use tools such as Ansible or Puppet to manage configuration and deployment. For example: `ansible-playbook -i hosts playbook.yml` will run an Ansible playbook on a set of hosts. Be aware of potential trade-offs between automation and flexibility.",
  "how do you troubleshoot a service that won t start": "To troubleshoot a service that won't start, check the system logs for error messages related to the service. For example: `systemctl status <service_name>` will display the status of the service. Be aware of potential production failure modes such as slow performance or data loss if the service is critical.",
  "difference between docker and virtual machine": "Docker and Virtual Machine are both containerization platforms, but Docker uses a lightweight virtualization layer to run containers, whereas VMs use full hardware emulation. This makes Docker more efficient in terms of resource usage.",
  "explain docker daemon": "The Docker daemon is the background process that manages Docker containers and images on the host system. It listens for API requests from the `docker` command-line tool and manages container lifecycle events.",
  "explain docker engine": "Docker Engine is a software platform that provides a runtime environment for Docker containers. It includes the Docker daemon, as well as tools such as `docker-compose` and `docker-machine`. The Docker Engine manages the creation, execution, and deletion of containers on the host system.",
  "difference between image and container": "A Docker Image is a template that defines an application's environment, including dependencies and configuration. A Container is an instance of a Docker Image, which runs as a process on the host system. This means that multiple Containers can be created from the same Image.",
  "explain docker networking": "Docker networking uses a combination of the host's network stack and a virtual network to enable communication between containers. Containers can be connected to a bridge network, which allows them to communicate with each other and the host. The `docker run` command can specify a custom network using the `-net` flag.",
  "bridge vs host networking": "Bridge networking is the default mode in Docker and provides isolation between containers while still allowing communication with the host. Host networking, on the other hand, allows containers to use the host's IP address and port range, but can introduce security risks if not properly configured.",
  "explain docker volumes": "Docker volumes are directories that persist data even after a container is deleted. They can be created using `docker volume create` or by mounting an existing directory with the `-v` flag in `docker run`. Volumes provide persistence and sharing of data between containers.",
  "explain bind mounts": "Bind mounts are a type of Docker volume that mount a host directory into a container. They can be created using the `-v` flag in `docker run`, specifying the path to the host directory. Bind mounts provide direct access to host files and directories.",
  "how do you reduce docker image size": "To reduce Docker image size, use multi-stage builds with `ARG` variables to separate build-time dependencies from runtime dependencies. Use `COPY --from=stage` instead of `RUN` commands to copy artifacts between stages. Remove unnecessary packages and files using `apt-get purge` or `yum remove`. Use a smaller base image, such as `alpine`, for the final stage.",
  "how do you debug a running container": "To debug a running container, use `docker logs` to view container output, `docker exec` to run commands inside the container, and `docker inspect` to examine container metadata. Use `docker stats` to monitor resource usage and identify potential issues.",
  "explain namespaces": "Namespaces in Docker provide isolation between containers by separating their process trees, network stacks, and mount points. They can be used to prevent privilege escalation attacks and improve container security. The `docker run` command can specify a custom namespace using the `-net-namespace` flag.",
  "explain cgroups": "Cgroups in Docker provide resource isolation between containers by limiting CPU, memory, and I/O usage. They can be used to prevent resource starvation attacks and improve container security. The `docker run` command can specify custom cgroup settings using the `-cgroup` flag.",
  "explain control plane": "The Control Plane is the central component of a Kubernetes cluster, responsible for managing the state of the cluster. It consists of several components: API Server, Controller Manager, Scheduler, and Etcd. The Control Plane ensures that the cluster remains in a desired state by reconciling actual state with the intended state defined in the Kubernetes configuration files.",
  "explain worker node": "A Worker Node is an instance of a machine that runs containerized applications managed by the Kubernetes control plane. Each node has a unique identifier and can be either a master or worker node, depending on its role within the cluster. A worker node typically runs multiple pods, each containing one or more containers.",
  "explain api server": "The API Server is the core component of the Control Plane that exposes the Kubernetes API to clients. It provides a RESTful interface for interacting with the cluster and manages access control through authentication and authorization mechanisms. The API Server is responsible for validating and processing requests, as well as updating the cluster state accordingly.",
  "explain scheduler": "The Scheduler is responsible for scheduling new pods onto nodes within the cluster based on resource availability, affinity rules, and other constraints. It uses a scoring system to evaluate potential placements and selects the most suitable node for each pod. The Scheduler can be configured using command-line flags or configuration files.",
  "explain controller manager": "The Controller Manager is responsible for managing the state of the cluster by reconciling actual state with intended state defined in Kubernetes configuration files. It runs controllers, such as ReplicaSet, Deployment, and StatefulSet, which ensure that the desired state is maintained. The Controller Manager can be configured using command-line flags or configuration files.",
  "explain kubelet": "The kubelet is an agent running on each node within a cluster, responsible for managing the lifecycle of containers on that node. It communicates with the API Server to receive instructions and updates the node's status accordingly. The kubelet ensures that containers are running as expected and restarts them if necessary.",
  "explain kube proxy": "kube-proxy is a network proxy component in Kubernetes that provides load balancing, service discovery, and network policies for pods within a cluster. It runs on each node and maintains a local cache of the cluster's IP address table. The kube-proxy can be configured using command-line flags or configuration files.",
  "difference between deployment and replicaset": "A Deployment is responsible for managing replicas of an application, ensuring that a specified number of replicas are running at any given time. A ReplicaSet is a lower-level resource that manages pods and ensures that a specified number of replicas are running. The key difference between the two is that a Deployment can be used to manage multiple ReplicaSets.",
  "difference between statefulset and deployment": "A StatefulSet is designed for applications that require persistent storage, such as databases or file systems. It provides ordered, unique network identifiers (UIDs) and ensures that pods are started and scaled in a specific order. A Deployment, on the other hand, is used for stateless applications and does not provide these guarantees.",
  "difference between daemonset and deployment": "A DaemonSet is responsible for running one instance of a pod on each node within a cluster. It is typically used for system-level services or monitoring tools that require access to all nodes in the cluster. A Deployment, by contrast, manages replicas of an application and can be scaled up or down as needed.",
  "how do you secure docker containers": "To secure Docker containers, use least privilege principles by running containers as non-root users. Use `docker run --user` to specify a custom user ID and group ID for the container. Implement network policies using `docker network create` and `docker network connect`. Regularly update and patch the host operating system.",
  "difference between compose and kubernetes": "Compose is a tool for defining and running multi-container Docker applications, while Kubernetes is a container orchestration platform that automates deployment, scaling, and management of containers. Compose provides a simpler way to manage small-scale applications, whereas Kubernetes is designed for large-scale, distributed systems.",
  "your application is containerized and deployed using aws services would you use github actions or argo cd for deployment and why": "I would choose GitHub Actions for deployment. It integrates well with AWS services and provides a seamless experience for automating the build, test, and deploy process. I can leverage the `aws` action to interact with AWS services, such as ECR and ECS. For example, I can use the `aws.ecr.get-login-password` action to authenticate with ECR and push the Docker image. Additionally, GitHub Actions provides a built-in feature for handling rollbacks and blue-green deployments.",
  "if the application is running on fargate instead of eks would your deployment tool choice change": "If the application is running on Fargate instead of EKS, my deployment-tool choice would change. In this case, I would choose Argo CD. Argo CD provides better support for serverless applications and can handle the complexities of deploying to Fargate. It also integrates well with AWS services such as ECS and ECR.",
  "how would you design a pipeline where every feature branch commit triggers build unit tests and static analysis but the docker image is pushed to ecr only after a pr is approved and merged": "To design a pipeline where every feature-branch commit triggers build, unit tests, and static analysis, but the Docker image is pushed to ECR only after a PR is approved and merged, I would use GitHub Actions. Here's an example of how I would configure it: `on: [push] steps: - name: Build and test uses: actions/checkout@v2 run: make build && make test - name: Static analysis uses: actions/checkout@v2 run: make static-analysis - name: Push to ECR if PR is approved and merged uses: aws-actions/configure-aws-credentials@v1 env: AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }} AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }} AWS_REGION: us-west-2 run: echo 'Approved and merged' | grep -q true && aws ecr get-login-password --region us-west-2 | docker login -u AWS --password-stdin <account-id>.dkr.ecr.<aws-region>.amazonaws.com && docker tag <image-name> <account-id>.dkr.ecr.<aws-region>.amazonaws.com/<image-name>:latest && docker push <account-id>.dkr.ecr.<aws-region>.amazonaws.com/<image-name>:latest",
  "what trigger condition would you use when a pr is created from a feature branch to dev": "When a PR is created from a feature branch to dev, I would use the `pull_request` trigger condition with the following configuration: `on: pull_request: branches: [dev] types: [opened]",
  "if a pr is already open and the test pipeline fails how do you trigger the pipeline again after fixing the code": "To retrigger the pipeline after fixing code in an open PR, use the `github.retriggers` action with the `event` set to `pull_request`. For example: `github.retriggers@main event=pull_request`. This will trigger a new run of the pipeline using the latest changes. Production failure modes include unnecessary re-runs causing increased build times and costs. Security/reliability concerns involve ensuring the retriggered pipeline uses the correct credentials and environment variables.",
  "what is the difference between synchronize and reopened events in github pull requests": "In GitHub pull requests, `synchronize` events occur when a PR is updated with new commits, while `reopened` events happen when a PR is reopened after being closed. The key difference lies in their triggers: synchronize events are triggered by changes to the code, whereas reopened events are triggered by manual actions (e.g., reopening a closed PR).",
  "when would you reopen a pr and how would you do it": "You would reopen a PR when you want to re-evaluate or re-discuss changes made since it was last reviewed. To reopen a PR, navigate to the pull request in GitHub, click on the three dots at the top right corner, and select 'Reopen'. This will put the PR back into an open state for further review.",
  "in github actions how would you add a manual approval between terraform plan and terraform apply": "To add a manual approval between terraform plan and terraform apply in GitHub Actions, use the `needs` keyword to specify that one action depends on another. For example: `needs: [manual-approval]`. Then, create a separate job with the `manual-approval` name and include an approval step using the `github.actions.manualApproval` action.",
  "in what format does the end user reviewer provide approval": "The end user/reviewer provides approval in a pull request comment using the `/approve` or `@reviewer approve` syntax.",
  "can approval notifications be integrated with email slack or teams": "Yes, approval notifications can be integrated with email, Slack, and Teams. For example, you can use the Jenkins Email Extension plugin to send emails when a build is approved or denied. Similarly, you can integrate with Slack using the Jenkins Slack Plugin.",
  "how does manual approval work in jenkins": "In Jenkins, manual approval works through the 'Manual Approval' plugin. When enabled, it adds an approval step to your pipeline that requires explicit approval from a reviewer before proceeding. The reviewer receives a notification and can approve or deny the build using the Jenkins UI.",
  "if jenkins accepts approve and deny but someone enters okay what happens": "If someone enters 'Okay' instead of 'Approve' or 'Deny', it will be treated as an invalid input, and the pipeline will fail. You should ensure that reviewers understand the correct syntax to use for approval and denial.",
  "how would you restrict jenkins approval input to only valid values": "To restrict Jenkins approval input to only valid values, use the `enum` validation in the Jenkinsfile. For example: `properties { pipeline { stages { stage('Approval') { steps { input message: 'Please approve this', id: 'approval', parameters: [[$class: 'BooleanParameterDefinition', description: '', name: 'approval']]] } } } }`. This will restrict the approval input to only true or false values.",
  "if a pipeline generates an artifact and you need to store it externally would you choose nexus or jfrog artifactory": "I would choose JFrog Artifactory over Nexus for storing artifacts externally. Artifactory provides more features such as artifact resolution, proxying, and caching, which can improve pipeline performance and reduce network latency. Additionally, Artifactory supports more formats like NuGet, Maven, and Gradle.",
  "after building and tagging a docker image how do you verify connectivity between the pipeline and jfrog": "To verify connectivity between the pipeline and JFrog after building and tagging a Docker image, use the `docker push` command with the JFrog repository URL. For example: `docker push <account_name>.jfrog.io/<repo_name>/<image_name>:<tag>`. If the push is successful, it indicates that the pipeline can connect to JFrog.",
  "how do you authenticate and push a docker image to jfrog artifactory": "To authenticate and push a Docker image to JFrog Artifactory, use the `docker login` command with your JFrog credentials. Then, use the `docker tag` command to add the JFrog repository URL to your image. Finally, use the `docker push` command to push the image to JFrog. For example: `docker login <account_name>.jfrog.io -u <username> -p <password>; docker tag my-image <account_name>.jfrog.io/<repo_name>/my-image:latest; docker push <account_name>.jfrog.io/<repo_name>/my-image:latest`.",
  "what does build once promote many mean in an artifact management strategy": "In an artifact-management strategy, 'build once, promote many' refers to the practice of building and storing artifacts (e.g., JARs, WARs) in a central location, such as Artifactory or Nexus, and then promoting them to multiple environments (e.g., dev, staging, prod). This approach ensures consistency across environments and reduces the risk of version drift.",
  "how would you deploy a public facing static website in aws": "To deploy a public-facing static website in AWS, create an S3 bucket for hosting the website content. Configure CloudFront as a CDN to distribute the content globally. Use Route 53 for DNS management. Obtain an SSL/TLS certificate from ACM (AWS Certificate Manager) and configure it with CloudFront.",
  "do you require acm when using s3": "No, you do not require ACM when using S3. However, if your website is publicly accessible and requires HTTPS, you should obtain an SSL/TLS certificate from a trusted provider or use AWS Certificate Manager (ACM). Without ACM, you may need to manually upload certificates to S3.",
  "if a custom domain is required what additional aws services would you use": "If a custom domain is required for the static website, use Route 53 as the DNS service. Obtain an SSL/TLS certificate from ACM and configure it with CloudFront. Use CloudFront's custom domain feature to map your custom domain to the S3 bucket hosting your website content.",
  "from a security standpoint how would you secure a public facing static website": "To secure a public-facing static website, I would configure an SSL/TLS certificate using AWS Certificate Manager (ACM) and enable HTTPS redirection. This involves creating an ACM certificate for the domain, configuring Route 53 to use the certificate, and setting up CloudFront with the ACM certificate. Additionally, I would ensure that sensitive data is not stored in S3 buckets and implement access controls such as IAM policies and bucket policies.",
  "how does cloudfront help with security apart from being a cdn": "CloudFront helps with security by providing a web application firewall (WAF) and DDoS protection. It also supports SSL/TLS encryption at the edge locations, which reduces the attack surface of the origin server. Furthermore, CloudFront can be configured to block common web attacks such as SQL injection and cross-site scripting.",
  "how does an ssl tls certificate improve security": "An SSL/TLS certificate improves security by encrypting data in transit between the client's browser and the server. This prevents eavesdropping and tampering with sensitive information. The certificate also authenticates the identity of the website, preventing man-in-the-middle attacks. To implement an SSL/TLS certificate, I would use a tool like OpenSSL to generate a private key and certificate signing request (CSR), then obtain a certificate from a trusted authority.",
  "what type of acm certificate would you use for a public custom domain": "For a public custom domain, I would use an ACM Certificate of type 'Public SSL/TLS certificate'. This type of certificate is issued by AWS and can be used with Route 53 and CloudFront. To create the certificate, I would follow these steps: 1) Go to the AWS Management Console and navigate to the ACM dashboard. 2) Request a public certificate for the domain. 3) Verify domain ownership using DNS or email validation.",
  "how do you map the acm certificate with cloudfront": "To map an ACM certificate with CloudFront, you need to create a new distribution and specify the ACM certificate ARN under 'SSL/TLS' settings. Then, update the DNS records for your domain to point to CloudFront's CNAMEs. The command to create a new distribution is: `aws cloudfront create-distribution --origin-id <OriginId> --enabled-variants '*' --default-cache-behavior 301`. Make sure to replace `<OriginId>` with the ID of your S3 bucket.",
  "how do you map a custom domain to artifacts stored in s3": "To map a custom domain to artifacts stored in S3, you need to create an alias for your S3 bucket and configure Route 53 to point to it. The command to create an alias is: `aws s3api put-bucket-website --bucket <BucketName> --website-configuration '<WebsiteConfiguration>'`. Replace `<BucketName>` with the name of your S3 bucket, and `<WebsiteConfiguration>` with the configuration for your website. Then, update the DNS records for your custom domain to point to Route 53's CNAMEs.",
  "what is the complete request flow when a user opens a static website hosted using s3 and cloudfront": "The complete request flow when a user opens a static website hosted using S3 and CloudFront is as follows: 1) The user requests the URL from their browser. 2) The request goes to Route 53, which resolves the domain name to an IP address of a CloudFront edge server. 3) The request then goes to the CloudFront edge server, which caches the response if it exists. If not, it forwards the request to S3. 4) S3 serves the static website content and returns it to CloudFront. 5) CloudFront caches the response and returns it to the user's browser.",
  "how does route 53 participate in the request flow": "Route 53 participates in the request flow by resolving the domain name to an IP address of a CloudFront edge server. When you create a hosted zone for your domain, Route 53 generates four CNAME records that point to CloudFront's edge servers. You need to update your DNS settings with these CNAME records so that when a user requests your website, their request goes through Route 53 and gets routed to the nearest CloudFront edge server.",
  "what happens when a user sends hundreds or thousands of requests continuously to the website": "When a user sends hundreds or thousands of requests continuously to the website, it can lead to increased latency and potential errors due to S3's eventual consistency model. To mitigate this, consider using CloudFront with an edge location cache, which can reduce the load on S3 and improve performance. Additionally, monitor metrics such as 4xx error rates and request latency in AWS CloudWatch to detect potential issues.",
  "how would you protect the website from abusive traffic or ddos style request patterns": "To protect the website from abusive traffic or DDoS-style request patterns, use AWS Shield Standard for DDoS protection, which provides automatic detection and mitigation of common network attacks. Additionally, configure S3 bucket policies to restrict access based on IP addresses or user agents, and consider using AWS WAF (Web Application Firewall) to filter out malicious requests.",
  "a customer has a restrictive on prem network and needs to access applications running in aws what connectivity services would you use": "To connect a customer's restrictive on-prem network to applications running in AWS, use Direct Connect for high-bandwidth, low-latency connectivity. Alternatively, use Site-to-Site VPN for secure, encrypted connections over the internet. Consider using Transit Gateway as a hub-and-spoke architecture for multiple on-prem networks.",
  "when would you use site to site vpn versus direct connect": "Use Site-to-Site VPN when you need to connect a single on-prem network to AWS, and want to leverage existing infrastructure such as firewalls and routers. Use Direct Connect when high-bandwidth, low-latency connectivity is required, or when you need to connect multiple on-prem networks to AWS through a Transit Gateway.",
  "where would aws privatelink fit in this architecture": "AWS PrivateLink would be used for service integration with on-premises resources, allowing them to communicate securely without exposing them directly to the internet. This is typically done using AWS PrivateLink's VPC interface endpoints or network load balancers.",
  "what are the other vpn types besides site to site vpn": "Other VPN types include Client-to-Site VPN (AWS Client VPN), which allows remote users to connect to AWS resources securely, and Transit Gateway Network Load Balancer (TGNLB) VPN, which enables secure communication between multiple VPCs or on-premises networks.",
  "what is the difference between site to site vpn and aws client vpn": "The key difference is that Site-to-Site VPN connects entire networks, whereas AWS Client VPN connects individual users. Site-to-Site VPN is typically used for remote offices or branch offices connecting to a central hub, while AWS Client VPN is designed for remote work scenarios.",
  "what inputs are required from the customer side to configure a site to site vpn": "To configure a Site-to-Site VPN, the customer needs to provide their public IP address, subnet mask, and pre-shared key (PSK) or certificate information. They also need to specify the VPCs and subnets that should be connected across the VPN tunnel.",
  "what inputs and components are required on the aws side": "To establish a hybrid connectivity solution between on-prem and AWS, you'll need an AWS VPN Gateway (VGW) as the central hub. On the AWS side, you'll also require a Virtual Private Cloud (VPC), subnets, route tables, security groups, network ACLs (NACLs), and an internet gateway or NAT instance for outbound traffic. Additionally, ensure that your on-premises VPN device is compatible with AWS VGW.",
  "once the vpn tunnel is established how does traffic flow inside aws": "Once the VPN tunnel is established, traffic flows inside AWS through the VPC routing table. The route table directs traffic to the correct subnet based on the destination IP address. Traffic then passes through security groups and NACLs for access control and filtering before reaching its final destination within the VPC or being routed out to the internet via an internet gateway or NAT instance.",
  "how do route tables security groups nacls and internal load balancers participate in the request flow": "In a hybrid connectivity scenario, route tables direct traffic between subnets. Security Groups (SG) filter traffic at the instance level, while NACLs control inbound and outbound traffic at the subnet level. Internal load balancers distribute traffic across instances within a VPC. For example, if an on-prem instance sends traffic to an AWS EC2 instance, the route table directs it to the correct subnet, then SG filters the traffic, followed by NACLs, before reaching the internal load balancer.",
  "what should be verified for the return traffic from aws back to on prem": "To verify return traffic from AWS back to on-prem, ensure that the VPN tunnel is established and the routing tables are correctly configured. Verify that the security groups and NACLs allow inbound traffic from the AWS VPC to the on-premises network. Additionally, check that the on-premises VPN device can establish a connection with the AWS VGW and that the return traffic is being routed correctly through the VPN tunnel.",
  "are you comfortable writing terraform scripts": "Yes, I am comfortable writing Terraform scripts. I have experience with Terraform configuration language and have used it to manage infrastructure as code in production environments.",
  "have you worked with terraform modules": "Yes, I have worked with Terraform modules. Modules allow for reusable and modular infrastructure definitions, which can improve maintainability and reduce duplication of effort. For example, you can use the `aws_instance` module to create an EC2 instance.",
  "how would you create 10 ec2 instances using terraform": "To create 10 EC2 instances using Terraform, I would define a resource block with the `count` argument set to 10 and reference it in a `resource` block. Here's an example: `resource \"aws_instance\" \"example\" { count = 10 instance_type = \"t2.micro\" }`. This will create 10 instances with the specified type.",
  "when would you use count versus for each": "I would use `count` when creating a large number of identical resources, such as EC2 instances. In contrast, I would use `for_each` when creating resources that have unique attributes or require different configurations. For example, if you need to create multiple instances with different instance types, `for_each` is more suitable.",
  "if you use modules how do you pass a value from one terraform module to another": "You can pass a value from one Terraform module to another using input variables. In the child module, declare an input variable with a default value or no default value if it's required. Then, in the parent module, use the `variable` block to assign a value to that input variable.",
  "how do you pass an output from a vpc module into an ec2 module": "To pass an output from a VPC module into an EC2 module, you can use Terraform's output values. In the VPC module, declare an output value using the `output` block and assign it a value. Then, in the EC2 module, use the `value` attribute to reference that output value.",
  "from parent root module to child module what mechanism do you use to pass values": "From parent/root module to child module, you can pass values using Terraform's input variables or by using the `module` block with the `input` argument. The `input` argument allows you to pass a map of values to the child module.",
  "if dev qa and prod use the same terraform code but different values how do you segregate those values": "To segregate dev, QA, and prod values in Terraform code, use environment-specific variables or configuration files like Terraform Cloud's workspace variables or AWS Systems Manager Parameter Store. You can then reference these variables in your Terraform code using the `env` function or by importing them as input variables.",
  "how do you pass environment specific values during provisioning": "You can pass environment-specific values during provisioning using Terraform's built-in `terraform.tfvars` file. Create separate files for each environment, e.g., `dev.tfvars`, `qa.tfvars`, and `prod.tfvars`. Then, in your main configuration, use the `file()` function to load the environment-specific variables, like this: `variable 'db_password' { default = file('./env/${terraform.env}/db_password.txt') }`.",
  "how do you separate terraform state for dev qa and prod": "To separate Terraform state for dev, QA, and prod, use a different backend configuration for each environment. Create separate files for each environment's backend configuration in the `backend.tf` file, like this: `terraform { backend \"s3\" { bucket = \"my-bucket-dev\" key    = \"path/to/dev/state.tfstate\" } }`. Then, run Terraform with the `-chdir` option to specify the correct backend configuration for each environment.",
  "if a variable has a default value in variables tf and a different value in tfvars which one is used": "If a variable has a default value in `variables.tf` and a different value in `.tfvars`, the value from `.tfvars` is used. This allows you to override default values with environment-specific settings.",
  "what happens if the same variable is explicitly set to an empty string in tfvars": "If the same variable is explicitly set to an empty string \"\" in `.tfvars`, Terraform will use the default value defined in `variables.tf`. However, be aware that setting a variable to an empty string can lead to unexpected behavior or errors if not properly handled by your configuration.",
  "where do you store your terraform backend state file": "We store our Terraform backend/state file in a secure, version-controlled repository such as GitLab or GitHub. The configuration is stored in the root directory of the repository and is named `terraform.tf`.",
  "what happens if a terraform state file in a gcs bucket is accidentally deleted and object versioning is not enabled": "If a Terraform state file in a GCS bucket is accidentally deleted and object versioning is not enabled, Terraform will fail to apply changes due to a mismatch between the local state and the remote state. This can lead to data loss or corruption.",
  "how would you recover from a deleted terraform state file": "To recover from a deleted Terraform state file, we would use the `terraform init` command with the `-migrate-state` option to migrate the state to a new location, such as a new GCS bucket. We would also restore the deleted state file from backups or version control.",
  "what preventive controls would you enable for terraform state": "To prevent data loss and ensure reliability, we enable object versioning for Terraform state files stored in GCS buckets. This allows us to recover deleted state files and maintain a consistent view of infrastructure configuration across teams.",
  "what challenges did you face while working with terraform the landing zone platform": "I faced challenges with [PROJECT NAME] on [PLATFORM] due to [CHALLENGE TYPE], such as inconsistent state or resource drift. To mitigate these issues, I ensured that [CONFIGURATION] was properly set up and that [MONITORING] was in place to detect and alert on potential problems.",
  "what happens if the terraform state gets corrupted": "If the Terraform state gets corrupted, it can lead to inconsistent infrastructure configurations and potential resource duplication. To recover, I would first attempt to restore the state from a backup using `terraform init --input=false` and then `terraform apply`. If no backup is available, I would manually reconcile the infrastructure using `gcloud` commands and then re-run Terraform to ensure consistency.",
  "how would you recover terraform if you don t have a state backup": "To recover Terraform without a state backup, I would use the `terraform init` command with the `--replace-state` flag to recreate the state file. This will overwrite the existing state file, so it's essential to ensure that the infrastructure is in a consistent state before running this command. Alternatively, I would manually reconcile the infrastructure using `gcloud` commands and then re-run Terraform to ensure consistency.",
  "explain the gcp resource project hierarchy": "The GCP resource/project hierarchy consists of a project, which is the top-level container for resources. Within a project, there are folders, which are used to organize resources and apply access controls. Resources, such as Compute Engine instances or Cloud Storage buckets, are created within folders and are subject to the access controls and policies applied to the folder. This hierarchy allows for fine-grained control over resource access and management.",
  "what are the different types of gcp iam roles": "There are five types of IAM roles in GCP: Service Account, User, Service Account with Custom Token, Organization, and Project.",
  "explain basic primitive vs predefined vs custom iam roles": "Basic/Primitive roles are predefined roles that provide a set of permissions for common tasks. Predefined roles are also predefined but offer more fine-grained permissions. Custom roles can be created to provide a specific set of permissions for a particular task or resource.",
  "why wouldn t you create custom iam roles for everything": "You wouldn't create custom IAM roles for everything because it can lead to role sprawl and make it harder to manage permissions. Custom roles should be created only when necessary, and their permissions should be carefully reviewed to avoid over-permissioning.",
  "explain the gke architecture": "The GKE architecture consists of a control plane, data plane, and worker nodes. The control plane manages the cluster, while the data plane handles the workload, and the worker nodes run the application.",
  "what is the difference between terraform open source community usage and terraform enterprise": "Terraform Enterprise is a commercial version of Terraform that offers additional features, support, and tools compared to the open-source/community version. Terraform Enterprise includes features like enterprise-level security, advanced collaboration tools, and integration with other Google Cloud services. The open-source/community version of Terraform is free and open-source, with a large community of contributors and users.",
  "can a gcp project belong to multiple folders": "Yes, a GCP project can belong to multiple folders. A GCP project can have multiple folders, and each folder can have its own set of resources and policies. This allows for more granular access control and resource management.",
  "where does the billing account fit in the gcp hierarchy": "The Billing Account is the top-level entity in the GCP hierarchy, and it is used to manage billing and cost allocation across multiple projects and folders. It is not a project or folder, but rather a container that holds the billing configuration for a GCP organization.",
  "explain the gcp landing zone architecture that you have designed": "A GCP Landing Zone is composed of a Hub and Spoke model, where the Hub contains the shared resources and the Spoke projects are connected to the Hub through VPC Peering or Shared VPC.",
  "how do you organize folders in your gcp landing zone": "Folders in a GCP Landing Zone are organized using the hierarchical structure of the Hub and Spoke model, with shared resources in the Hub and project-specific resources in the Spoke. The organization is typically based on project or business unit.",
  "what did you set up on the governance side of the landing zone": "Governance in a GCP Landing Zone includes the setup of IAM policies, resource tags, and access controls, such as Identity and Access Management (IAM) roles and permissions. This ensures that resources are properly secured and monitored.",
  "when would you choose shared vpc vs vpc peering": "Shared VPC is chosen when multiple projects need to share the same VPC and IP address space, while VPC Peering is used when projects need to communicate with each other without sharing the same VPC.",
  "what is the difference between hpa and vpa": "Horizontal Pod Autoscaler (HPA) and Vertical Pod Autoscaler (VPA) are both used for autoscaling in Kubernetes. HPA scales the number of replicas of a pod based on CPU utilization, while VPA scales the resources (e.g., CPU and memory) allocated to a pod.",
  "in what situations would you use hpa": "Use HPA when you want to scale the number of replicas of a pod based on CPU utilization, such as in a web application with a large number of concurrent users.",
  "in what situations would you use vpa": "Use VPA when you want to scale the resources allocated to a pod, such as in a database that requires more memory as data grows.",
  "can we use hpa and vpa together": "HPA and VPA can be used together, but they serve different purposes. HPA is used for scaling the number of replicas, while VPA is used for scaling resources. You can use both together to scale both the number of replicas and the resources allocated to a pod.",
  "what governance iam roles did you configure for the landing zone": "You configured the following IAM roles for the Landing Zone: Project Owner, Project Editor, and Project Viewer. These roles are part of the Google Cloud IAM roles that are specifically designed for Landing Zone governance.",
  "explain the gke node pool lifecycle": "The GKE node pool lifecycle consists of the following stages: Preemption, Scaling, Maintenance, and Termination. The node pool can be preempted to make way for a larger node pool, scaled to increase or decrease the number of nodes, maintained to perform routine maintenance tasks, and terminated when the node pool is no longer needed.",
  "what does the kubernetes controller manager do": "The Kubernetes Controller Manager is responsible for managing the lifecycle of the cluster's control plane components, including the API server, scheduler, and etcd. It is also responsible for managing the cluster's identity and authentication.",
  "what is the difference between ingress and egress traffic": "Ingress and Egress traffic refer to the traffic that enters and leaves a network or a cluster. Ingress traffic is the traffic that enters a cluster, while Egress traffic is the traffic that leaves a cluster. The difference between the two is that Ingress traffic is typically controlled by the cluster's network policies, while Egress traffic is typically controlled by the cluster's security policies.",
  "what problems can occur when hpa and vpa both operate using cpu memory metrics": "When HPA and VPA both operate using CPU/memory metrics, it can lead to over- and under-scaling. HPA prioritizes CPU and memory usage, while VPA focuses on usage-based pricing. If both use the same metrics, HPA may scale up unnecessarily, while VPA may not scale down enough, causing resource waste and increased costs.",
  "if hpa scales pods and the cluster autoscaler scales nodes couldn t that cause unnecessary cluster scaling": "Yes, if HPA scales Pods and the Cluster Autoscaler scales nodes, it can cause unnecessary cluster scaling. This can lead to over-provisioning and increased costs. The Cluster Autoscaler should be used to scale the cluster based on demand, not HPA or VPA.",
  "how would you mitigate over scaling between hpa vpa cluster autoscaler": "To mitigate over-scaling, you can use a combination of HPA, VPA, and Cluster Autoscaler. HPA can scale Pods based on CPU and memory usage, while VPA can scale resources based on usage-based pricing. The Cluster Autoscaler can be used to scale the cluster based on demand, ensuring that resources are only provisioned when needed.",
  "can we implement cluster autoscaler together with hpa and vpa": "Yes, Cluster Autoscaler can be implemented together with HPA and VPA. In fact, it's recommended to use all three tools together to ensure that the cluster is scaled based on demand, while also considering usage-based pricing and resource utilization.",
  "a pod is stuck in pending state how would you troubleshoot it": "To troubleshoot a stuck Pod in Pending state, first check the Pod's status using `kubectl get pod <pod-name> -o json` to identify any errors or warnings. Then, use `kubectl describe pod <pod-name>` to examine the Pod's logs and configuration. If the issue persists, check the node's status using `kubectl get nodes` and ensure the node is running the correct Docker image. Verify the Pod's container runtime is functioning correctly using `kubectl get container <container-name> -o json`. If necessary, use `kubectl exec -it <pod-name> -- /bin/bash` to inspect the container's logs and configuration.",
  "what if nodes have enough cpu and memory but the pod is still pending": "If nodes have enough CPU and memory but the Pod is still Pending, check the Pod's container runtime configuration using `kubectl get container <container-name> -o json`. Verify the container's Docker image is correctly pulled and running using `kubectl get pod <pod-name> -o json`. Check the Pod's security context using `kubectl get pod <pod-name> -o json` to ensure the correct credentials are being used. Additionally, check the Pod's network configuration using `kubectl get pod <pod-name> -o json` to ensure proper communication with the node.",
  "how do jenkins agents work": "Jenkins agents work by connecting to a Jenkins master node using a secure connection (e.g., SSH). The agent downloads the necessary build artifacts and executes the build process. The agent reports back to the master node with the build results. Jenkins agents can be configured to run on multiple nodes, allowing for distributed builds and improved scalability.",
  "what is the difference between the jenkins controller and agent": "The Jenkins Controller and Agent are two separate components in a Jenkins cluster. The Controller manages the build process and schedules jobs, while the Agent executes the build process on a node. The Agent can be configured to run on multiple nodes, allowing for distributed builds and improved scalability. The Controller and Agent communicate using a secure connection (e.g., SSH).",
  "how does jenkins determine which agent should execute a job": "Jenkins determines which agent should execute a job based on the agent's availability and the job's requirements. It checks the agent's status and the job's configuration to select the best agent for the task.",
  "how do dynamic kubernetes jenkins agents work": "Dynamic Kubernetes Jenkins agents use the Kubernetes Service Broker API to manage agent connections. The Service Broker creates a new agent instance when a job is triggered, and the agent is configured to run in a Kubernetes pod.",
  "what happens when a jenkins agent goes down": "When a Jenkins agent goes down, Jenkins detects the agent's absence and removes it from the list of available agents. The job is then reassigned to another available agent, or the job is paused until the agent becomes available again.",
  "how do you configure alerts based on logs": "To configure alerts based on logs, you can use a logging tool like ELK (Elasticsearch, Logstash, Kibana) or Splunk. You can set up log forwarding to send logs to a centralized logging server, and then configure alerts using the logging tool's built-in alerting features, such as ELK's Logstash alerting plugin or Splunk's Alerting app.",
  "how would you create log based alerts in gcp": "To create log-based alerts in GCP, you can use Cloud Logging and Cloud Pub/Sub. You can create a Cloud Function that reads logs, extracts the relevant information, and publishes it to a Pub/Sub topic. Then, you can create a Cloud Pub/Sub subscription that listens to the topic and sends alerts to a notification channel.",
  "how would you alert on a specific error message in application logs": "To alert on a specific error message in application logs, you can use Cloud Logging and Cloud Functions. You can create a Cloud Function that reads logs, extracts the error message, and sends an alert to a notification channel. You can also use Cloud Logging's built-in filtering and aggregation features to narrow down the logs and reduce false positives.",
  "how do you avoid alert noise alert fatigue": "To avoid alert noise/alert fatigue, you can use techniques such as rate limiting, thresholding, and suppression. You can also use Cloud Logging's built-in features such as log filtering and aggregation to reduce the volume of logs that trigger alerts. Additionally, you can use Cloud Functions to create custom alerting logic that takes into account the context of the alert.",
  "you have a very large github repository that you re migrating to bitbucket and the migration is failing how would you troubleshoot fix it": "To troubleshoot/fix a failing migration of a large GitHub repository to Bitbucket, you can start by checking the migration logs for errors and exceptions. You can use GitHub's API to retrieve the repository's history and check for any errors during the migration process. You can also use Bitbucket's API to check for any errors during the migration process. Additionally, you can use a tool like Git LFS to check for any issues with large files during the migration process.",
  "why would you use git clone mirror git push mirror during repository migration": "You would use git clone --mirror / git push --mirror during repository migration to create a mirror of the original repository and then push the changes to the new repository, allowing for a smooth transition and minimizing data loss.",
  "how would you migrate git lfs objects": "To migrate Git LFS objects, you would use the git lfs fetch command to retrieve the LFS objects from the original repository, and then use the git lfs push command to push the LFS objects to the new repository.",
  "what would you do if large binary files in git history were causing the migration failure": "If large binary files in Git history were causing the migration failure, you would use the git filter-branch command with the --subdirectory-filter option to filter out the large binary files, or use the git filter-repo command to perform a more targeted filter.",
  "how does garbage collection in python work": "Python's garbage collection is a cycle collector that periodically frees up memory occupied by objects that are no longer referenced. It works by identifying unreachable objects and deleting them, and it is enabled by default in Python 2.x and 3.x.",
  "what is reference counting": "Reference counting is an algorithm used to manage memory by tracking the number of references to each object in memory. When the reference count reaches zero, the object is deallocated.",
  "how does python handle circular references": "Python uses a cycle detection algorithm to handle circular references. When the garbage collector detects a circular reference, it breaks the cycle by changing the references to point to a special object called a 'weakref'.",
  "what is generational garbage collection": "Generational garbage collection is a technique used to reduce the overhead of garbage collection by dividing objects into generations based on their lifetimes. Younger generations are collected more frequently than older generations.",
  "what is a variable in python": "In Python, a variable is an object that stores a value. Variables are created when they are assigned a value, and they can be reassigned to hold different values.",
  "what is the time complexity of removing duplicates using a seen set": "The time complexity of removing duplicates using a seen set is O(n), where n is the number of elements in the set.",
  "can xor be used for duplicate related problems": "No, XOR cannot be used for duplicate-related problems. XOR has a time complexity of O(n) for duplicate-related problems, which is the same as using a seen set.",
  "given an array where every number appears twice except one find the unique number using xor": "The unique number can be found by performing XOR on all numbers in the array. The result will be the unique number. The time complexity is O(n).",
  "when should you use xor vs set for duplicate unique element problems": "Use XOR when the order of elements does not matter, and use a set when the order of elements matters. XOR is more efficient when the number of unique elements is close to the total number of elements.",
  "what are the constraints rules for naming python variables": "In Python, variable names must start with a letter (a-z or A-Z), underscore (_), or dollar sign (#). They can contain letters, digits, underscores, and dollar signs, but cannot contain whitespace or special characters.",
  "can python reserved keywords be used as variable names": "No, Python reserved keywords cannot be used as variable names. They must be unique and not used as identifiers.",
  "what are mutable and immutable objects in python": "In Python, mutable objects are those that can be changed after creation, such as lists, dictionaries, and sets. Immutable objects are those that cannot be changed, such as integers, floats, and strings.",
  "give examples of mutable and immutable python objects": "Mutable Python objects can change in place: lists (`[1, 2]`), dictionaries (`{'a': 1}`), sets, bytearrays, and most user-defined class instances. Immutable objects cannot change after creation: integers, floats, booleans, strings, bytes, frozensets, and tuples whose element references are fixed. For example, `items.append(3)` mutates a list, while `name += 'x'` creates a new string object. Immutability does not automatically imply hashability: a tuple containing a list is immutable as a tuple but unhashable because the nested list is mutable.",
  "when you enter a command such as nslookup in linux and press enter what happens internally from shell to kernel": "The shell parses `nslookup`, performs expansions, and searches aliases, built-ins, functions, and then directories in `$PATH`. Because `nslookup` is an external executable, the shell normally calls `fork()` (or an optimized equivalent), and the child calls `execve()` to load the binary. The kernel creates the process image, maps the executable and shared libraries, and schedules it. `nslookup` then uses libc and system calls such as `socket()`, `sendto()`, and `recvfrom()` to query a configured DNS resolver. The kernel handles networking and returns results; the program exits and the parent shell collects its status with `waitpid()`.",
  "what are fork and execve": "fork() and execve() are two fundamental system calls in Linux. fork() creates a new process by duplicating an existing one, while execve() replaces the current process image with a new one. execve() takes a new program image as an argument, which is loaded into memory and executed. The kernel creates a new process context and switches to it, while fork() creates a new process context and returns it to the parent process.",
  "how does a user space process communicate with the linux kernel": "A user-space process requests kernel services through the system-call interface. A libc wrapper such as `read()`, `open()`, or `socket()` places the syscall number and arguments in registers and executes the architecture's trap instruction, such as `syscall` on x86-64. The CPU switches to kernel mode, the kernel validates pointers and permissions, performs the operation, and returns a value or sets `errno` through the wrapper. Processes also interact through memory-mapped files, `/proc`, `/sys`, device files, signals, sockets, and `ioctl()`, but those mechanisms ultimately rely on kernel-managed interfaces. User code cannot directly access privileged kernel memory or hardware.",
  "given a file get the 7th column of the 7th row using unix linux commands": "To get the 7th column of the 7th row of a file using Unix/Linux commands, you can use the following command: awk 'NR==7 {print $7}' file.txt. This command uses the awk command-line editor to extract the 7th column of the 7th row from the file. Note that this assumes that the file is in a format that can be parsed by awk. If the file is not in a format that can be parsed by awk, you may need to use a different command, such as cut or sed.",
  "what is the sed command used for": "`sed` is a non-interactive stream editor used to transform text from files or standard input. It can select lines, print or delete them, substitute text, insert or append content, and apply commands to matching address ranges. For example, `sed 's/error/warning/g' app.log` changes every match in the output, while `sed -i.bak 's/old/new/g' file` edits a file and keeps a backup. Without `-i`, the original file is unchanged. It is best for line-oriented transformations; use `grep` primarily for filtering and `awk` for field-oriented calculations and structured reports.",
  "how do you use sed to print a specific line": "Suppress normal output with `-n` and use the `p` command for the required address. To print line 7, run `sed -n '7p' file.txt`. To print a range, use `sed -n '7,10p' file.txt`; to print lines matching a pattern, use `sed -n '/ERROR/p' file.txt`. Without `-n`, `sed '7p'` prints every input line normally and prints line 7 a second time. For a large file, `sed` still reads sequentially, so `awk 'NR==7 { print; exit }' file.txt` can stop immediately after the target line.",
  "how do you use sed for search and replace": "Use the substitution command `s/pattern/replacement/flags`. For example, `sed 's/http:/https:/g' input.txt` replaces every match on each line and writes the result to standard output. To edit in place while retaining a backup, use `sed -i.bak 's/old/new/g' file.txt` on GNU sed; macOS syntax is `sed -i '.bak' ...`. Choose another delimiter for paths, such as `sed 's#/old/path#/new/path#g'`. Escape metacharacters carefully, test without `-i` first, and remember that basic versus extended regular-expression behavior varies; `-E` enables extended expressions on common implementations.",
  "what is the difference between the typical uses of grep awk and sed": "`grep` selects lines that match a pattern, for example `grep -i 'error' app.log`. `sed` performs line-oriented editing and transformations, such as `sed 's/old/new/g' file`. `awk` splits records into fields and supports conditions, arithmetic, aggregation, and formatted output, for example `awk '$3 > 80 { total += $3 } END { print total }' file`. Their capabilities overlap, but the usual choice is grep for filtering, sed for substitutions or line edits, and awk for column-aware processing. They compose naturally in pipelines, although one well-written awk program can often replace several processes.",
  "given a list containing duplicate elements remove the duplicates": "If order does not matter and elements are hashable, use `unique = list(set(values))`; this is concise but does not guarantee the original order. To preserve order, use `unique = list(dict.fromkeys(values))` on modern Python. An explicit version maintains a `seen` set and appends a value only when it has not been seen. The set-based ordered approach is O(n) average time and O(n) additional space. If elements are unhashable, such as lists or dictionaries, use a hashable key conversion or an equality-based scan with a possible O(n²) cost.",
  "how can you improve duplicate removal using a set": "Using a set to improve duplicate removal can be more efficient, but it may not preserve the original order of elements. To achieve this, you can use a combination of a set and a list:\n    def remove_duplicates_ordered(input_list):\n        return list(dict.fromkeys(input_list))\n\n# Example usage:\ninput_list = [1, 2, 2, 3, 4, 4, 5]\nprint(remove_duplicates_ordered(input_list))\n# Output: [1, 2, 3, 4, 5]",
  "given akhilesh kumar singh create a dictionary containing the frequency of each character": "Use `collections.Counter`, which returns a dictionary-like mapping: `from collections import Counter; frequencies = dict(Counter('Akhilesh Kumar Singh'))`. This counts characters exactly as written, so uppercase and lowercase letters are different and spaces are included. For a case-insensitive count without spaces, normalize first: `text = 'Akhilesh Kumar Singh'; frequencies = dict(Counter(ch.lower() for ch in text if not ch.isspace()))`. A manual loop using `freq[ch] = freq.get(ch, 0) + 1` produces the same result. Both approaches run in O(n) time and require O(k) space for k distinct characters.",
  "you have an employee table how would you create employee1 with the same columns structure but without copying data": "Use a schema-copy statement supported by the database. In MySQL, `CREATE TABLE employee1 LIKE employee;` copies columns and indexes without rows. A portable pattern is `CREATE TABLE employee1 AS SELECT * FROM employee WHERE 1 = 0;`, which creates the table from the result schema while the always-false predicate copies no data. The CTAS form may not preserve primary keys, foreign keys, indexes, defaults, identity properties, comments, or triggers, depending on the engine. For an exact structural clone, extract and review the original DDL, change the table name, and execute that DDL.",
  "is a tuple completely immutable if it contains a mutable object": "The tuple itself is immutable: its length and references cannot be changed after creation. However, an object referenced by the tuple can still be mutable. For example, `t = (1, [2, 3])`; `t[1].append(4)` is valid and produces `(1, [2, 3, 4])`, but `t[1] = []` raises `TypeError` because that would replace a tuple element. This distinction also affects hashability: a tuple is hashable only when every contained value is hashable, so a tuple containing a list cannot be used as a dictionary key or set member.",
  "what is the purpose of create table employee1 as select from employee where 1 0": "`CREATE TABLE employee1 AS SELECT * FROM employee WHERE 1 = 0;` creates a real, empty table whose columns are derived from the `employee` query. Because `1 = 0` is always false, no rows are copied. This is convenient for quickly copying column names and compatible data types, but CTAS behavior is database-specific and commonly omits constraints, primary and foreign keys, indexes, triggers, identity settings, defaults, privileges, and comments. If those objects must be preserved, use an engine-specific clone command such as MySQL's `CREATE TABLE employee1 LIKE employee`, or generate and edit the original table DDL.",
  "solve the above using not exists": "Return order-side customer IDs for which no matching customer exists: `SELECT DISTINCT o.customer_id FROM Orders_df o WHERE NOT EXISTS (SELECT 1 FROM Customers_df c WHERE c.customer_id = o.customer_id);`. The subquery is correlated through `c.customer_id = o.customer_id`; `NOT EXISTS` keeps the outer row only when that lookup finds nothing. `DISTINCT` prevents repeated IDs when a missing customer has multiple orders. This form is generally safer than `NOT IN` because a `NULL` in the customer table can make `NOT IN` evaluate to unknown. An index or key on `Customers_df.customer_id` improves lookup performance.",
  "solve the same problem using a left join": "Use an anti-join: `SELECT DISTINCT o.customer_id FROM Orders_df o LEFT JOIN Customers_df c ON c.customer_id = o.customer_id WHERE c.customer_id IS NULL;`. The left join keeps every order row; unmatched customer rows receive `NULL` values, and the `WHERE` clause selects only those unmatched rows. `DISTINCT` removes duplicate customer IDs caused by multiple orders. Test the nullable column from the right-hand table, preferably its non-null key. Indexing `customer_id` on both inputs helps larger datasets, and you should decide explicitly whether a `NULL` order-side customer ID should be reported or filtered out.",
  "for each city find the second highest order amount": "Use a per-city ranking when ties must be treated as the same amount: `WITH ranked AS (SELECT city, order_amount, DENSE_RANK() OVER (PARTITION BY city ORDER BY order_amount DESC) AS rnk FROM Orders_df) SELECT city, order_amount FROM ranked WHERE rnk = 2;`. `DENSE_RANK` returns the second distinct highest amount and can return multiple orders tied at that amount. If the requirement is the second physical row rather than the second distinct value, use `ROW_NUMBER` with a deterministic tie-breaker such as `order_id`. Clarify tie semantics before choosing the function.",
  "find the second highest order amount using dense rank": "Rank amounts within each city in descending order, then keep rank 2: `WITH ranked AS (SELECT city, order_id, order_amount, DENSE_RANK() OVER (PARTITION BY city ORDER BY order_amount DESC) AS amount_rank FROM Orders_df) SELECT city, order_id, order_amount FROM ranked WHERE amount_rank = 2;`. `DENSE_RANK` assigns the same rank to equal amounts and does not leave gaps, so this returns all orders tied at the second distinct highest amount. Remove `PARTITION BY city` if the question asks for the second-highest amount across the entire table.",
  "find the second highest order amount using max without a window function": "For the overall second distinct amount, use `SELECT MAX(order_amount) AS second_highest FROM Orders_df WHERE order_amount < (SELECT MAX(order_amount) FROM Orders_df);`. For each city, correlate the inner maximum: `SELECT o.city, MAX(o.order_amount) AS second_highest FROM Orders_df o WHERE o.order_amount < (SELECT MAX(i.order_amount) FROM Orders_df i WHERE i.city = o.city) GROUP BY o.city;`. Cities with fewer than two distinct amounts return no row. This approach handles ties because it explicitly excludes the maximum value, but a window function is often clearer and can return the matching order rows as well.",
  "find an order whose amount is greater than the average order amount of that specific customer": "Use a correlated subquery so each order is compared with its own customer's average: `SELECT o.* FROM Orders_df o WHERE o.order_amount > (SELECT AVG(i.order_amount) FROM Orders_df i WHERE i.customer_id = o.customer_id);`. The inner query is reevaluated logically for the current outer customer, although the optimizer may rewrite it. A scalable alternative pre-aggregates averages and joins them: `WITH a AS (SELECT customer_id, AVG(order_amount) avg_amount FROM Orders_df GROUP BY customer_id) SELECT o.* FROM Orders_df o JOIN a ON a.customer_id = o.customer_id WHERE o.order_amount > a.avg_amount;`. Index `customer_id` for large tables.",
  "find customers orders whose order amount is greater than the average order amount": "To compare every order with the overall average, use `SELECT o.* FROM Orders_df o WHERE o.order_amount > (SELECT AVG(order_amount) FROM Orders_df);`. The uncorrelated subquery produces one scalar value for the whole table, and every order is compared with it. If customer names are required, join `Customers_df c ON c.customer_id = o.customer_id`. This is different from comparing against each customer's own average, which requires a correlated subquery or grouped CTE. Decide how `NULL` amounts should be treated; `AVG` ignores them, and `NULL > average` is not true.",
  "how does a correlated subquery calculate the average separately for each customer": "The inner query references the current row from the outer query, usually by customer ID: `SELECT o.* FROM Orders_df o WHERE o.order_amount > (SELECT AVG(i.order_amount) FROM Orders_df i WHERE i.customer_id = o.customer_id);`. For each logical outer order, `o.customer_id` changes, so the subquery averages only rows belonging to that customer. Database optimizers may transform this into a join rather than literally rerunning it row by row. A grouped CTE joined on `customer_id` often makes the same logic clearer and more efficient. An index on `customer_id` helps, and customers with only one non-null order cannot exceed their own average.",
  "if a kubernetes pod is stuck in pending state or crashloopbackoff how would you troubleshoot it": "To troubleshoot a stuck Kubernetes pod in Pending or CrashLoopBackOff state, first check the pod's logs for any errors or exceptions. Then, verify the pod's configuration and ensure it meets the required specifications. Use the `kubectl describe pod <pod-name>` command to inspect the pod's status and logs. Next, check the pod's network connectivity and DNS resolution using `kubectl exec -it <pod-name> -- /bin/sh` and `dig <domain-name>`. If the issue persists, use `kubectl get pod <pod-name> --raw` to inspect the pod's container status and `kubectl describe deployment <deployment-name>` to check the deployment's configuration. Additionally, check the cluster's node and pod resource utilization using `kubectl top node` and `kubectl top pod`. Finally, consider running a pod with a similar configuration to reproduce the issue and use `kubectl debug` to debug the pod.",
  "what is the difference between gke standard and gke autopilot": "GKE Standard gives the customer control over node pools, machine types, node configuration, scaling bounds, upgrades, and many cluster-level settings; billing is largely based on provisioned infrastructure. Autopilot has Google provision and manage nodes from workload requests, enforces stronger workload and security defaults, and primarily bills for requested Pod resources, subject to its pricing rules. Autopilot reduces operational effort but restricts privileged workloads, direct node access, and some low-level customization. Choose Standard for specialized node or platform requirements and Autopilot when workload portability and reduced cluster administration matter more. In both modes, Google manages the control plane while the customer owns application configuration and data.",
  "explain the elk stack flow": "The ELK stack is a popular data analytics platform consisting of Elasticsearch, Logstash, and Kibana. The flow works as follows: Elasticsearch stores and indexes log data, Logstash collects and processes data from various sources, and Kibana provides a user-friendly interface for visualizing and analyzing data. The data flows from Logstash to Elasticsearch, where it is indexed and stored. Kibana then retrieves the data from Elasticsearch and displays it in a graphical format, allowing users to query and analyze the data.",
  "if elasticsearch elk is running out of disk space what would you do": "First confirm node filesystem usage, shard allocation, index growth, and Elasticsearch low/high/flood-stage disk watermarks using `_cat/allocation`, `_cat/indices`, and cluster allocation explanations. Stop nonessential ingestion if capacity is critical, remove only expired indices through an approved ILM retention policy, and take verified snapshots before destructive cleanup. Then expand the existing volume when its StorageClass supports expansion, or add data nodes and let Elasticsearch rebalance shards. Do not casually delete shard files or set replicas to zero. After recovery, restore write access if flood-stage protection marked indices read-only, validate cluster health, and add capacity, watermark, and ingestion-rate alerts.",
  "can we attach another persistent volume pvc to an elasticsearch pod when it is running out of disk space": "Not as a simple way to enlarge Elasticsearch's existing data path. Kubernetes can mount multiple PVCs only when the Pod or StatefulSet specification and application layout are designed for them; `kubectl attach` does not attach storage. The safer options are to expand the existing PVC when `allowVolumeExpansion` and the CSI driver support it, then verify filesystem growth, or add a new Elasticsearch data node with its own PVC and allow shard rebalancing. Changing a StatefulSet volume claim template may require a controlled rollout and careful operator-specific steps. Never mount a second volume over the current data directory or manually move shard files, because that risks corruption and broken cluster metadata.",
  "how is dynatrace different from prometheus and grafana": "Dynatrace is a comprehensive monitoring and analytics platform that provides real-time visibility into application performance, user experience, and infrastructure health. It differs from Prometheus and Grafana in its ability to provide end-to-end visibility across the entire application stack, including user behavior, transactional data, and infrastructure metrics. Prometheus is a monitoring system that focuses on collecting and storing metrics, while Grafana is a visualization tool for displaying those metrics. Dynatrace, on the other hand, provides a more holistic view of the application, including the ability to analyze user behavior and identify performance bottlenecks.",
  "how do you monitor an application using dynatrace": "Deploy OneAgent on hosts or the Dynatrace Operator/activeGate integration for Kubernetes, scope it with least-privilege credentials, and verify that processes, services, traces, metrics, logs, and Kubernetes entities are discovered. Instrument browser or mobile traffic with Real User Monitoring and configure synthetic checks for critical journeys. Define management zones, ownership tags, service-level objectives, anomaly-detection settings, and alerting profiles routed to the responsible team. Use distributed traces and service flow to follow a slow request across dependencies, then correlate response time, failure rate, throughput, resource saturation, deployments, and logs. Validate data privacy rules, sampling, retention, and alert behavior before production rollout.",
  "given orders df and customers df find customer ids that exist in orders df but not in customers df": "This is a left anti-join. In SQL, use `SELECT DISTINCT o.customer_id FROM Orders_df o WHERE NOT EXISTS (SELECT 1 FROM Customers_df c WHERE c.customer_id = o.customer_id);`. In pandas, use `missing = Orders_df.loc[~Orders_df['customer_id'].isin(Customers_df['customer_id']), ['customer_id']].drop_duplicates()`. The SQL `NOT EXISTS` form avoids the `NULL` behavior that can make `NOT IN` unexpectedly return no rows. Decide whether a null order-side customer ID represents invalid data and should be included or filtered. For large SQL tables, index or cluster both datasets on `customer_id`; in pandas, align data types before comparing.",
  "if city is directly available in orders df solve the second highest problem without joining customers df": "No customer join is required because the grouping key is already in `Orders_df`. Use `WITH ranked AS (SELECT city, order_id, order_amount, DENSE_RANK() OVER (PARTITION BY city ORDER BY order_amount DESC) AS rnk FROM Orders_df) SELECT city, order_id, order_amount FROM ranked WHERE rnk = 2;`. This returns every order tied at the second distinct highest amount in each city. If only one row per city is required, clarify the tie rule and use `ROW_NUMBER()` with a deterministic secondary order such as `order_id`. Cities with fewer than two distinct amounts produce no rank-2 result."
}
