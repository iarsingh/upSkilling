---
title: "Sample Post — Replace or Delete via the Admin Panel"
category: "DevOps"
excerpt: "This is a starter template showing the Markdown/MDX format the blog expects. Replace it with your first real post, or delete it from /admin."
date: "2026-01-01"
tags: ["Template"]
draft: true
---

This post exists to demonstrate the blog's Markdown/MDX format and frontmatter fields (`title`, `category`, `excerpt`, `date`, `tags`, `draft`).

## Structure

- Front matter drives the list view, category filter, and reading-time estimate.
- The body below the `---` fence is standard Markdown/MDX — headings, code blocks, lists, and links all work.
- Set `draft: true` to hide a post from the public list while you're still writing it.

```bash
# Code blocks render with syntax highlighting
kubectl get pods -n platform
```

Replace this file's content, or use the admin panel at `/admin` to add your first real post — no code changes required.
